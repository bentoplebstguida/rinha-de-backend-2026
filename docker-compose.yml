version: "3.8"

services:
  api1:
    build:
      context: ./api
      platforms:
        - linux/amd64
    container_name: api1
    environment:
      - MODE=api
      - SOCKET_PATH=/sockets/api1.sock
    volumes:
      - submission_sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: "160MB"

  api2:
    build:
      context: ./api
      platforms:
        - linux/amd64
    container_name: api2
    environment:
      - MODE=api
      - SOCKET_PATH=/sockets/api2.sock
    volumes:
      - submission_sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: "160MB"

  lb:
    build:
      context: ./api
      platforms:
        - linux/amd64
    container_name: lb
    ports:
      - "9999:9999"
    environment:
      - MODE=lb
      - SOCKETS=/sockets/api1.sock,/sockets/api2.sock
    volumes:
      - submission_sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: "30MB"
    depends_on:
      - api1
      - api2

volumes:
  submission_sockets:
